# Rules

## Shell

Never use PowerShell. Use Bash for all shell commands.
Keep Bash usage simple — avoid commands that require user
authorization prompts. Prefer native file tools (Read, Edit,
Write, Glob, Grep) over shell commands whenever possible.
